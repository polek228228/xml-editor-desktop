# 🚀 Автоматическая сборка Windows .exe через GitHub Actions

## Что это?

GitHub Actions - это **бесплатная виртуальная Windows машина** в облаке GitHub.
Она автоматически соберет полноценный `.exe` NSIS installer с native модулями.

**Преимущества:**
- ✅ Бесплатно (для публичных репозиториев)
- ✅ Настоящая Windows среда
- ✅ Автоматическая сборка при каждом релизе
- ✅ Native модули (sqlite3, libxmljs2) компилируются корректно
- ✅ Не нужен доступ к физической Windows машине

---

## 📋 Предварительные требования

1. **GitHub репозиторий**
   - Проект должен быть на GitHub
   - Может быть публичным или приватным

2. **Git инициализирован** ✅ (уже есть)

---

## 🔧 Как использовать

### Вариант 1: Ручной запуск (рекомендуется для теста)

1. **Запушить код на GitHub:**
   ```bash
   git add .
   git commit -m "feat: Add GitHub Actions for Windows build"
   git push origin main
   ```

2. **Открыть GitHub:**
   - Перейти на https://github.com/[ваш-юзернейм]/xmlPZ
   - Кликнуть вкладку **"Actions"**

3. **Запустить сборку:**
   - Выбрать workflow **"Build Windows Installer"**
   - Нажать **"Run workflow"** → **"Run workflow"**
   - Подождать 5-10 минут

4. **Скачать .exe:**
   - Когда сборка завершится (зеленая галочка ✅)
   - Кликнуть на имя запуска
   - Скроллить вниз → секция **"Artifacts"**
   - Скачать **windows-installer.zip**
   - Внутри будет `XMLEditorDesktop-Setup-1.0.0.exe`

---

### Вариант 2: Автоматическая сборка при релизе

1. **Создать тег версии:**
   ```bash
   git tag v1.0.0
   git push origin v1.0.0
   ```

2. **GitHub автоматически:**
   - Запустит сборку
   - Создаст Release с .exe файлом
   - Файл будет доступен на странице Releases

3. **Скачать .exe:**
   - Перейти на https://github.com/[ваш-юзернейм]/xmlPZ/releases
   - Открыть Release v1.0.0
   - Скачать `XMLEditorDesktop-Setup-1.0.0.exe`

---

## 📦 Что будет собрано

```
XMLEditorDesktop-Setup-1.0.0.exe  (размер ~110-150 MB)
```

Это полноценный NSIS installer с:
- ✅ Графическим мастером установки
- ✅ Выбором пути установки
- ✅ Регистрацией в "Программы и компоненты"
- ✅ Ярлыками на рабочем столе и в меню Пуск
- ✅ Удалением через Control Panel
- ✅ Всеми зависимостями (sqlite3, libxmljs2, schemas)
- ✅ Автообновлением (если настроить)

---

## 🔍 Как это работает

Файл `.github/workflows/build-windows.yml` содержит инструкции:

```yaml
1. Запустить виртуальную Windows машину (windows-latest)
2. Установить Node.js 18
3. Установить зависимости (npm ci)
4. Собрать .exe (npm run build:win)
5. Загрузить артефакты
6. Создать Release (если был создан тег)
```

**Время сборки:** 5-10 минут

---

## 💰 Стоимость

**Для публичных репозиториев:** БЕСПЛАТНО (unlimited)

**Для приватных репозиториев:**
- GitHub Free: 2000 минут/месяц (бесплатно)
- Одна сборка = ~10 минут
- Можно собирать 200 раз в месяц

---

## 🐛 Устранение проблем

### Проблема: Actions не работает

**Решение:** Проверить что Actions включены:
1. Settings → Actions → General
2. "Allow all actions and reusable workflows"

---

### Проблема: Сборка падает с ошибкой

**Решение:** Посмотреть логи:
1. Кликнуть на запуск с красным крестиком ❌
2. Открыть "Build Windows installer"
3. Прочитать ошибку

Частые причины:
- Отсутствует package.json
- Некорректный build:win скрипт
- Проблемы с зависимостями

---

### Проблема: .exe не работает на Windows

**Решение:** Убедиться что:
1. Пользователь запускает на Windows 10/11 (64-bit)
2. Антивирус не блокирует
3. Скачан полный файл (~110-150 MB)

---

## 📝 Альтернативные способы

### 1. Wine на macOS (не рекомендуется)

```bash
# Установить Wine
brew install --cask wine-stable

# Попробовать собрать
WINEARCH=win64 npm run build:win
```

**Проблемы:**
- Часто падает с ошибками
- Native модули не компилируются
- Сложная настройка

### 2. Docker с Windows контейнером (не работает на macOS)

Docker Desktop на macOS не поддерживает Windows контейнеры.

### 3. Виртуальная машина Windows

- Установить Parallels/VMware с Windows 10
- Собрать там напрямую
- Долго и требует места (~30 GB)

---

## ✅ Рекомендация

**Используйте GitHub Actions** - это:
- ✅ Самый простой способ
- ✅ Бесплатно
- ✅ Надежно (настоящая Windows)
- ✅ Автоматизировано

---

## 📚 Полезные ссылки

- GitHub Actions: https://docs.github.com/en/actions
- electron-builder: https://www.electron.build/
- NSIS: https://nsis.sourceforge.io/

---

## 🚀 Быстрый старт

```bash
# 1. Запушить код
git add .github/workflows/build-windows.yml
git commit -m "feat: Add GitHub Actions for Windows build"
git push origin main

# 2. Создать тег (опционально)
git tag v1.0.0
git push origin v1.0.0

# 3. Открыть GitHub → Actions → Подождать 5-10 минут → Скачать .exe
```

---

## ✨ Готово!

Теперь у вас есть автоматическая сборка Windows installer! 🎉
