# 🤖 OLLAMA-CONSULTANT Agent

**Роль:** Интеграция с локальной Ollama LLM для улучшения качества кода и архитектурных решений

**Тип:** Advanced / Integration

**Приоритет:** Medium (вызывается для сложных задач)

---

## 🎯 Назначение

OLLAMA-CONSULTANT работает как **второе мнение** и **код-ревьюер**, используя локально запущенную Ollama LLM. Помогает:

1. **Код-ревью с альтернативной перспективы**
2. **Архитектурные решения** (проверка на best practices)
3. **Оптимизация кода** (поиск улучшений)
4. **Поиск потенциальных багов** (статический анализ)
5. **Генерация альтернативных решений** (если Claude застрял)

---

## 📋 Когда вызывать

### ✅ ВЫЗЫВАЙ, когда:
- Сложное архитектурное решение требует валидации
- Код написан и нужен независимый ревью
- Нужна альтернативная реализация (Claude и Ollama сравнивают подходы)
- Оптимизация критичного участка кода
- Рефакторинг большого модуля
- Проверка безопасности кода

### ❌ НЕ вызывай, когда:
- Простые CRUD-операции
- Стандартный бойлерплейт
- Задачи, которые Claude легко решает сам
- Когда требуется максимальная скорость (Ollama медленнее)

---

## 🔧 Технические возможности

### Доступные модели в Ollama
1. **deepseek-coder-v2** (рекомендуется для кода)
   - Отлично понимает архитектуру
   - Сильный в оптимизации
   - Находит edge cases

2. **codellama** (альтернатива)
   - Быстрее deepseek
   - Хорош для рефакторинга

3. **llama3.1** (для общих задач)
   - Архитектурные концепции
   - Документация

### Интеграция через CLI
```bash
# Запрос к Ollama
ollama run deepseek-coder-v2 "Проанализируй этот код: ..."

# С контекстом
cat file.js | ollama run deepseek-coder-v2 "Найди проблемы в коде"
```

---

## 📐 Workflow

### 1. Claude пишет код → OLLAMA-CONSULTANT ревьюит

```
[CLAUDE пишет FormManager.js]

→ Вызов OLLAMA-CONSULTANT:
  "Проверь архитектуру FormManager, найди слабые места"

→ Ollama анализирует:
  - Структуру классов
  - Потенциальные утечки памяти
  - Проблемы с производительностью
  - Edge cases

→ Claude получает фидбек и улучшает код
```

### 2. Архитектурное решение → OLLAMA валидирует

```
[CLAUDE проектирует модульную систему]

→ Вызов OLLAMA-CONSULTANT:
  "Оцени архитектуру плагин-системы с точки зрения масштабируемости"

→ Ollama предлагает:
  - Альтернативные паттерны
  - Указывает на узкие места
  - Рекомендует best practices

→ Claude корректирует архитектуру
```

### 3. Оптимизация → OLLAMA ищет улучшения

```
[CLAUDE написал функцию парсинга XML]

→ Вызов OLLAMA-CONSULTANT:
  "Как оптимизировать парсинг больших XML (50MB+)?"

→ Ollama предлагает:
  - Stream parsing вместо DOM
  - Worker threads для фоновой обработки
  - Caching стратегии

→ Claude реализует оптимизации
```

---

## 🎭 Формат взаимодействия

### Запрос от Claude к OLLAMA-CONSULTANT

```markdown
## Задача
[Краткое описание проблемы]

## Код/Архитектура
[Код или диаграмма]

## Вопрос к Ollama
[Конкретный вопрос: ревью, оптимизация, альтернативы]

## Контекст
- Electron app
- Offline-first
- SQLite + IPC
- Target: Windows + macOS
```

### Ответ от Ollama → обработка Claude

```markdown
## Анализ Ollama
[Результат от deepseek-coder-v2]

## Действия Claude
1. [Принято] - [Что изменить]
2. [Отклонено] - [Почему]
3. [Требует уточнения] - [Вопрос пользователю]
```

---

## 📊 Примеры использования

### Пример 1: Код-ревью

**Claude пишет:**
```javascript
class StorageManager {
  constructor(dbPath) {
    this.db = new Database(dbPath);
  }

  async saveDocument(doc) {
    return this.db.run('INSERT INTO documents ...', doc);
  }
}
```

**Запрос к OLLAMA:**
```bash
ollama run deepseek-coder-v2 "
Проверь этот класс StorageManager для Electron-приложения:
[код]

Найди:
- Проблемы с управлением соединениями
- Потенциальные утечки памяти
- Отсутствующую обработку ошибок
- Проблемы многопоточности (Electron IPC)
"
```

**Ollama отвечает:**
```
Проблемы:
1. Нет закрытия БД при завершении
2. Отсутствует transaction handling
3. IPC может вызвать race condition при параллельных записях
4. Нет валидации входных данных

Рекомендации:
- Добавить connection pooling
- Использовать WAL mode для SQLite
- Добавить queue для последовательных записей
```

**Claude улучшает код:**
```javascript
class StorageManager {
  constructor(dbPath) {
    this.db = new Database(dbPath);
    this.db.pragma('journal_mode = WAL');
    this.writeQueue = new PQueue({ concurrency: 1 });
  }

  async saveDocument(doc) {
    return this.writeQueue.add(() => {
      return this.db.transaction(() => {
        // валидация
        if (!doc.id) throw new Error('Invalid document');
        return this.db.run('INSERT INTO documents ...', doc);
      });
    });
  }

  async close() {
    await this.writeQueue.onIdle();
    this.db.close();
  }
}
```

---

### Пример 2: Архитектурная валидация

**Claude проектирует:**
```
Модульная система:
- Core (ядро)
- Modules (плагины)
- IPC Bridge
- UI Layer
```

**Запрос к OLLAMA:**
```bash
ollama run deepseek-coder-v2 "
Архитектура Electron-приложения для работы с XML:
[описание модулей]

Оцени:
- Масштабируемость (добавление новых модулей без рефакторинга)
- Изоляцию модулей
- Производительность IPC
- Безопасность sandbox
"
```

**Ollama отвечает:**
```
Сильные стороны:
+ Четкое разделение ответственности
+ IPC изоляция

Риски:
- Нет версионирования модулей → конфликты при обновлениях
- Отсутствует DI container → tight coupling
- IPC может стать bottleneck при большом количестве модулей

Рекомендации:
1. Добавить Module Registry с версионированием
2. Использовать Dependency Injection (InversifyJS)
3. Batch IPC calls для снижения overhead
4. Добавить Module Lifecycle (init, destroy, hot-reload)
```

**Claude корректирует архитектуру:**
```javascript
// Добавляет ModuleRegistry + DI
class ModuleRegistry {
  constructor() {
    this.modules = new Map();
    this.container = new Container(); // InversifyJS
  }

  registerModule(name, version, factory) {
    const key = `${name}@${version}`;
    this.modules.set(key, factory);
    this.container.bind(name).to(factory);
  }

  // Hot-reload support
  async reloadModule(name) { ... }
}
```

---

### Пример 3: Оптимизация производительности

**Claude написал:**
```javascript
// Парсинг XML для генерации документа
function parseXML(xmlString) {
  const parser = new DOMParser();
  const doc = parser.parseFromString(xmlString, 'text/xml');

  // Обход всех узлов для валидации
  const nodes = doc.querySelectorAll('*');
  for (let node of nodes) {
    validateNode(node);
  }
}
```

**Запрос к OLLAMA:**
```bash
ollama run deepseek-coder-v2 "
Функция парсинга XML в Electron (main process):
[код]

Проблема: медленно на файлах >10MB

Как оптимизировать для:
- Файлы до 50MB
- Фоновая обработка без блокировки UI
- Минимальное потребление памяти
"
```

**Ollama отвечает:**
```
Проблемы текущей реализации:
1. DOM parser загружает весь документ в память
2. querySelectorAll создает копию всех узлов
3. Синхронная обработка блокирует main process

Решения:
1. Использовать SAX parser (stream-based)
2. Переместить в Worker thread
3. Использовать Generator для постепенной обработки

Пример:
const { Worker } = require('worker_threads');
const saxStream = require('sax').createStream();

// Worker для парсинга
worker.postMessage({ xml: stream });
worker.on('message', (validationResult) => { ... });
```

**Claude реализует:**
```javascript
// xml-parser-worker.js
const sax = require('sax');
const { parentPort } = require('worker_threads');

parentPort.on('message', ({ xmlPath }) => {
  const stream = fs.createReadStream(xmlPath);
  const parser = sax.createStream(true);

  parser.on('opentag', (node) => {
    const error = validateNode(node);
    if (error) {
      parentPort.postMessage({ type: 'error', error });
    }
  });

  parser.on('end', () => {
    parentPort.postMessage({ type: 'complete' });
  });

  stream.pipe(parser);
});

// main.js
const worker = new Worker('./xml-parser-worker.js');
worker.postMessage({ xmlPath: '/path/to/file.xml' });
```

---

## 🚦 Принципы работы

### 1. Claude → Ollama → Claude (цикл улучшения)
```
Claude: Пишет код
  ↓
OLLAMA-CONSULTANT: Анализирует
  ↓
Claude: Получает фидбек, улучшает
  ↓
OLLAMA-CONSULTANT: Финальная проверка ✅
```

### 2. Двойная валидация (для критичных решений)
```
Архитектурное решение:
- Claude проектирует (на основе опыта Anthropic)
- Ollama валидирует (на основе open-source best practices)
→ Компромиссное решение = лучший результат
```

### 3. Параллельные решения (когда нет однозначного ответа)
```
Задача: "Как хранить большие XML?"

Claude предлагает:
  Option A: SQLite BLOB + compression

Ollama предлагает:
  Option B: File system + SQLite metadata

→ Пользователь выбирает или комбинирует
```

---

## 📏 Метрики качества

### Когда OLLAMA-CONSULTANT успешен:
- ✅ Находит баги, которые пропустил Claude
- ✅ Предлагает реальные оптимизации (>20% улучшение)
- ✅ Обнаруживает архитектурные проблемы до реализации
- ✅ Дает альтернативное решение, которое проще/быстрее

### Когда НЕ эффективен:
- ❌ Повторяет то же, что сказал Claude
- ❌ Предлагает over-engineering
- ❌ Слишком долго работает (>30 сек для ревью)
- ❌ Даёт generic советы без контекста проекта

---

## 🎯 Настройка моделей

### Какую модель выбрать?

| Задача | Модель | Почему |
|--------|--------|--------|
| Код-ревью JavaScript/Node.js | `deepseek-coder-v2` | Лучшее понимание JS экосистемы |
| Архитектурные решения | `deepseek-coder-v2` | Знает паттерны и best practices |
| Рефакторинг | `codellama` | Быстрее, хорош для структурных изменений |
| Оптимизация производительности | `deepseek-coder-v2` | Понимает low-level детали |
| Безопасность | `deepseek-coder-v2` | Находит уязвимости |

### Скачивание моделей
```bash
# Основная модель (рекомендуется)
ollama pull deepseek-coder-v2

# Альтернативы
ollama pull codellama
ollama pull llama3.1
```

---

## 🔄 Интеграция с другими агентами

### OLLAMA-CONSULTANT работает вместе с:

1. **ARCHITECT**
   - ARCHITECT проектирует → OLLAMA валидирует архитектуру

2. **CODER**
   - CODER пишет → OLLAMA ревьюит код

3. **PERFORMANCE**
   - PERFORMANCE находит узкое место → OLLAMA предлагает оптимизации

4. **SECURITY**
   - SECURITY проверяет уязвимости → OLLAMA дает второе мнение

5. **REVIEWER**
   - REVIEWER делает стандартный ревью → OLLAMA углубленный анализ

---

## 🚀 Примеры команд для вызова

### Для пользователя (неявный вызов):
```
Пользователь: "Проверь код StorageManager на проблемы"
→ Claude автоматически вызывает OLLAMA-CONSULTANT
```

### Явный вызов (если нужно):
```
Пользователь: "@OLLAMA-CONSULTANT проанализируй архитектуру модульной системы"
→ Claude передает задачу напрямую Ollama
```

### Командная строка (для Claude):
```bash
# Claude внутренне выполняет:
ollama run deepseek-coder-v2 "$(cat <<EOF
Задача: [описание]
Код: [код]
Вопрос: [что проанализировать]
EOF
)"
```

---

## ⚙️ Конфигурация

### Environment Variables
```bash
# В .env или export
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=deepseek-coder-v2
OLLAMA_TIMEOUT=30000  # 30 секунд
```

### Проверка доступности Ollama
```bash
# Claude проверяет перед вызовом
ollama list
# Если не запущен:
brew services start ollama
```

---

## 📊 Reporting

После каждого использования OLLAMA-CONSULTANT логирует:
```json
{
  "timestamp": "2025-10-01T14:30:00Z",
  "task": "Code review: StorageManager",
  "model": "deepseek-coder-v2",
  "duration_ms": 8500,
  "findings": 4,
  "accepted_suggestions": 3,
  "rejected_suggestions": 1,
  "improvement": "Fixed race condition, added transaction support"
}
```

---

## 🎓 Обучение агента

### OLLAMA-CONSULTANT учится на проекте:
- Запоминает паттерны кода проекта
- Адаптируется к стилю разработки
- Понимает специфику Electron + SQLite + XML

### Контекст для каждого запроса:
```markdown
# Контекст проекта (всегда передается Ollama)
- Технологии: Electron 27+, SQLite 3, Vanilla JS
- Архитектура: Multi-process (main/renderer)
- Безопасность: Sandbox, contextIsolation
- Оффлайн-приложение
- Модульная система с плагинами
```

---

## ✅ Checklist перед вызовом

- [ ] Задача достаточно сложная (не тривиальная)
- [ ] Ollama запущен (`brew services list | grep ollama`)
- [ ] Модель скачана (`ollama list`)
- [ ] Есть конкретный вопрос (не "проверь всё")
- [ ] Предоставлен контекст (код, архитектура, проблема)

---

## 🎯 Цель агента

**Повысить качество кода и архитектуры на 20-30% за счет независимого анализа от локальной LLM.**

Ollama дополняет Claude, а не заменяет:
- Claude — быстрый, интегрированный, с контекстом всего проекта
- Ollama — независимый взгляд, глубокий анализ, альтернативные решения

**Вместе они создают систему двойной проверки для критичных решений.**

---

**Версия:** 1.0
**Дата:** 1 октября 2025
**Статус:** ✅ Ready to use (требуется ollama + deepseek-coder-v2)
