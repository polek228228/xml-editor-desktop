# 🚀 Ollama Setup — Быстрый старт

**Статус:** ✅ Ollama установлен и запущен
**Версия:** 0.12.3

---

## ✅ Что уже сделано

1. ✅ Ollama установлен через Homebrew
2. ✅ Сервис запущен (`brew services start ollama`)
3. ✅ Агент OLLAMA-CONSULTANT создан

---

## 📦 Следующий шаг: Скачать модель

### Рекомендуемые модели для кода

#### 1. **DeepSeek Coder V2** (РЕКОМЕНДУЕТСЯ)
```bash
ollama pull deepseek-coder-v2:16b
```

**Характеристики:**
- Размер: ~9.5 GB
- Лучший для: JavaScript, TypeScript, Node.js, Electron
- Скорость: средняя (зависит от Mac)
- Качество: ⭐⭐⭐⭐⭐

**Время скачивания:** 10-30 минут (зависит от интернета)

---

#### 2. **CodeLlama** (Альтернатива, быстрее)
```bash
ollama pull codellama:13b
```

**Характеристики:**
- Размер: ~7.3 GB
- Лучший для: рефакторинг, общие задачи
- Скорость: быстрее DeepSeek
- Качество: ⭐⭐⭐⭐

---

#### 3. **Qwen2.5-Coder** (Легковесная)
```bash
ollama pull qwen2.5-coder:7b
```

**Характеристики:**
- Размер: ~4.7 GB
- Лучший для: быстрые проверки, если мало места
- Скорость: самая быстрая
- Качество: ⭐⭐⭐

---

## 🚀 Как выбрать модель?

| Если у тебя... | Выбирай |
|----------------|---------|
| Mac M1/M2/M3 с 16GB+ RAM | `deepseek-coder-v2:16b` |
| Mac M1/M2/M3 с 8GB RAM | `codellama:13b` |
| Нужна скорость | `qwen2.5-coder:7b` |
| Лучшее качество кода | `deepseek-coder-v2:16b` |

---

## 🔧 Команды для скачивания

### Вариант 1: DeepSeek Coder V2 (лучшее качество)
```bash
cd /Users/PotapovViS/Downloads/Discord-Telegram-Bridge-development/xmlPZ
ollama pull deepseek-coder-v2:16b
```

### Вариант 2: CodeLlama (быстрая альтернатива)
```bash
ollama pull codellama:13b
```

### Вариант 3: Qwen (легковесная)
```bash
ollama pull qwen2.5-coder:7b
```

---

## ✅ После скачивания

### Проверь, что модель скачалась:
```bash
ollama list
```

Должен увидеть:
```
NAME                      ID            SIZE      MODIFIED
deepseek-coder-v2:16b    abc123...     9.5 GB    2 minutes ago
```

### Протестируй модель:
```bash
ollama run deepseek-coder-v2:16b "Напиши функцию для валидации email на JavaScript"
```

Если выдаёт код — всё работает! ✅

---

## 🤖 Как Claude будет использовать Ollama

После скачивания модели, Claude автоматически будет вызывать OLLAMA-CONSULTANT для:

1. **Код-ревью сложных участков**
   ```
   Claude пишет код → OLLAMA-CONSULTANT проверяет → Claude улучшает
   ```

2. **Архитектурных решений**
   ```
   ARCHITECT проектирует → OLLAMA валидирует → Claude корректирует
   ```

3. **Оптимизации производительности**
   ```
   PERFORMANCE находит узкое место → OLLAMA предлагает решения
   ```

4. **Поиска багов**
   ```
   DEBUGGER анализирует → OLLAMA ищет edge cases
   ```

---

## 📊 Примеры работы

### Пример 1: Простой запрос
```bash
ollama run deepseek-coder-v2:16b "Проверь этот код на баги:

function saveDocument(doc) {
  db.run('INSERT INTO documents VALUES (?)', doc.id);
}
"
```

**Ollama ответит:**
```
Проблемы:
1. Нет обработки ошибок (try-catch)
2. Отсутствует валидация doc.id
3. SQL injection если doc.id не escaped
4. Нет проверки успешности операции

Исправленный код:
function saveDocument(doc) {
  if (!doc || !doc.id) {
    throw new Error('Invalid document');
  }

  try {
    const result = db.prepare('INSERT INTO documents VALUES (?)').run(doc.id);
    return result.changes > 0;
  } catch (error) {
    console.error('Save failed:', error);
    throw error;
  }
}
```

---

### Пример 2: Архитектура
```bash
ollama run deepseek-coder-v2:16b "Оцени архитектуру модульной системы:

Core → загружает модули
Modules → регистрируются в Core
IPC → общение между модулями

Найди проблемы масштабируемости"
```

**Ollama ответит:**
```
Проблемы:
1. Tight coupling - модули зависят от Core напрямую
2. Нет версионирования модулей
3. IPC bottleneck при большом количестве модулей
4. Отсутствует lifecycle management

Рекомендации:
- Dependency Injection для изоляции
- Module Registry с версионированием
- Event Bus вместо прямого IPC
- Lifecycle hooks (init, destroy, reload)
```

---

## 🔥 Горячие клавиши Ollama

```bash
# Остановить генерацию
Ctrl + C

# Выйти из интерактивного режима
/bye

# Показать статистику модели
/show

# Очистить контекст
/clear
```

---

## 🛠️ Troubleshooting

### Ollama не отвечает
```bash
# Перезапусти сервис
brew services restart ollama

# Проверь статус
brew services list | grep ollama
```

### Модель скачивается слишком долго
```bash
# Скачивай меньшую модель
ollama pull qwen2.5-coder:7b
```

### "Not enough memory"
```bash
# Используй квантизованную версию (q4)
ollama pull deepseek-coder-v2:16b-q4

# Или меньшую модель
ollama pull codellama:7b
```

---

## 📈 Производительность на Mac

| Mac | Модель | Скорость (tokens/sec) |
|-----|--------|----------------------|
| M3 Max 16GB | deepseek-coder-v2:16b | ~40-50 |
| M2 Pro 16GB | deepseek-coder-v2:16b | ~30-40 |
| M1 8GB | codellama:13b | ~20-30 |
| M1 8GB | qwen2.5-coder:7b | ~40-50 |

---

## ✅ Готов к работе

После скачивания модели:

1. ✅ Ollama установлен
2. ✅ Сервис запущен
3. ✅ Модель скачана
4. ✅ OLLAMA-CONSULTANT готов

**Можешь начинать разработку!**

Claude будет автоматически использовать Ollama для улучшения качества кода.

---

## 🎯 Рекомендация

**Скачай `deepseek-coder-v2:16b`** — это лучшая модель для нашего проекта:
```bash
ollama pull deepseek-coder-v2:16b
```

Пока скачивается (10-30 минут), можешь начинать разработку — Ollama будет доступен сразу после скачивания.

---

**Дата:** 1 октября 2025
**Статус:** ✅ Ollama готов, осталось скачать модель
