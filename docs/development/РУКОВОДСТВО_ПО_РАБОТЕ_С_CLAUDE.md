# 🤖 Руководство по работе с Claude + Система делегирования агентам

**Дата:** 1 октября 2025
**Версия:** 1.0
**Для:** Разработчик (ты)

---

## 🎯 Цель этого документа

Объяснить **как правильно работать со мной (Claude)**, чтобы я **автоматически делегировал задачи специализированным агентам** и ты получал максимальную производительность.

**Главное правило:** Ты пишешь задачу → Я анализирую → Вызываю нужных агентов → Выдаю результат.

---

## 📊 Общая схема взаимодействия

```
                  ┌──────────────┐
                  │     ТЫ       │
                  │ (разработчик)│
                  └───────┬──────┘
                          │
                    [Короткий запрос]
                    "надо форму"
                          │
                          ▼
          ┌───────────────────────────────┐
          │         CLAUDE CODE           │
          │  (оркестратор агентов)        │
          └───────────┬───────────────────┘
                      │
        ┌─────────────┼─────────────┐
        │ Анализирую задачу         │
        │ Определяю агента          │
        │ Делегирую                 │
        └─────────────┬─────────────┘
                      │
     ┌────────────────┼────────────────┐
     │                │                │
     ▼                ▼                ▼
┌─────────┐    ┌──────────┐    ┌──────────┐
│ PROMPT- │    │ UI-      │    │ CODER    │
│OPTIMIZER│ →  │ DESIGNER │ →  │          │
└─────────┘    └──────────┘    └────┬─────┘
                                     │
                              [Результат]
                                     │
                                     ▼
                          ┌──────────────────┐
                          │   REVIEWER       │
                          │   (проверка)     │
                          └────────┬─────────┘
                                   │
                            [Финальный код]
                                   │
                                   ▼
                          ┌──────────────────┐
                          │      ТЕБЕ        │
                          │   (результат)    │
                          └──────────────────┘
```

**Ты не вызываешь агентов вручную!** Я делаю это автоматически.

---

## 🚀 Как правильно писать запросы

### ✅ Хорошие запросы (короткие, конкретные)

**Примеры:**
```
"Создай форму для ПЗ v01.05"
"Не работает сохранение в базу"
"Оптимизируй запросы к SQLite"
"Добавь валидацию ИНН"
"Сделай модуль экспертизы"
"Напиши тесты для FormManager"
```

**Что я делаю:**
1. Анализирую запрос
2. Если неясно → вызываю **PROMPT-OPTIMIZER** (он уточнит детали)
3. Выбираю нужного агента
4. Делегирую задачу
5. Возвращаю тебе результат

---

### ❌ Плохие запросы (слишком общие)

**Примеры:**
```
"Доделай проект"              → Непонятно, что именно
"Исправь баги"                → Какие баги? Где?
"Сделай красиво"              → Что красиво? Какой модуль?
"Улучши код"                  → Какой код? Как улучшить?
```

**Что я сделаю:**
Вызову **PROMPT-OPTIMIZER**, который задаст уточняющие вопросы:
```
🤔 Уточнения:
- Какой именно модуль нужно доделать?
- Какие конкретно баги (номера, описание)?
- Что означает "красиво" (дизайн, код, архитектура)?
```

После твоих ответов → делегирую агентам.

---

## 🤖 Система агентов — как это работает

### 17 специализированных агентов

У меня есть **17 агентов** (это специальные промпты в `.md` файлах), каждый эксперт в своей области:

#### 🛠️ Core (Разработка) — 5 агентов
1. **ARCHITECT** — проектирует архитектуру (классы, модули, связи)
2. **CODER** — пишет код (JS, HTML, CSS, SQL)
3. **REVIEWER** — делает код-ревью (находит проблемы, улучшения)
4. **DEBUGGER** — ищет баги (анализирует ошибки, предлагает фикс)
5. **REFACTORER** — рефакторит код (убирает дублирование, улучшает структуру)

#### 🎨 Design (Дизайн) — 2 агента
6. **UI-DESIGNER** — проектирует интерфейсы (макеты, HTML/CSS)
7. **UX-ANALYST** — анализирует удобство (user flow, проблемы UX)

#### ✅ Quality (Качество) — 3 агента
8. **TESTER** — пишет тесты (unit, E2E)
9. **PERFORMANCE** — оптимизирует (скорость, память, размер)
10. **SECURITY** — проверяет безопасность (уязвимости, best practices)

#### 📝 Content — 1 агент
11. **DOCUMENTER** — пишет документацию (README, JSDoc, guides)

#### 🔧 Helpers — 2 агента
12. **PROMPT-OPTIMIZER** ⭐ — улучшает твои короткие запросы (КЛЮЧЕВОЙ!)
13. **TASK-SPLITTER** — разбивает большие задачи на подзадачи

#### ⚡ Advanced — 5 агентов
14. **WEB-RESEARCHER** — ищет решения в интернете (best practices 2024-2025)
15. **CODE-GENERATOR** — генерирует код из шаблонов (в 5x быстрее)
16. **ARCHITECTURE-VALIDATOR** — проверяет соблюдение архитектуры
17. **AUTO-TESTER** — автоматически создаёт тесты
18. **OLLAMA-CONSULTANT** 🤖 — локальная LLM для независимого код-ревью

---

## 🔄 Автоматическое делегирование — как я выбираю агента

### Правило 1: По ключевым словам в запросе

| Твой запрос содержит | Я вызываю агента |
|---------------------|------------------|
| "создать", "сделать", "напиши" | **CODER** |
| "не работает", "ошибка", "баг" | **DEBUGGER** |
| "оптимизируй", "ускорь", "медленно" | **PERFORMANCE** |
| "форма", "UI", "интерфейс", "дизайн" | **UI-DESIGNER** → **CODER** |
| "тест", "проверь", "покрытие" | **TESTER** |
| "архитектура", "как организовать" | **ARCHITECT** |
| "документация", "README", "гайд" | **DOCUMENTER** |
| "рефактор", "улучши структуру" | **REFACTORER** |
| "код-ревью", "проверь код" | **REVIEWER** |
| "безопасность", "уязвимость" | **SECURITY** |

### Правило 2: По сложности задачи

**Простая задача (1-2 агента):**
```
Ты: "Добавь валидацию email"
Я:  CODER → пишет код
```

**Средняя задача (3-4 агента):**
```
Ты: "Создай форму регистрации"
Я:  UI-DESIGNER → макет
    ↓
    CODER → код
    ↓
    REVIEWER → проверка
```

**Сложная задача (5+ агентов):**
```
Ты: "Сделай модуль экспертизы"
Я:  TASK-SPLITTER → разбивает на 8 подзадач
    ↓
    ARCHITECT → проектирует архитектуру
    ↓
    CODE-GENERATOR → генерирует каркас
    ↓
    UI-DESIGNER → дизайн форм
    ↓
    CODER → реализация
    ↓
    TESTER → тесты
    ↓
    REVIEWER → ревью
    ↓
    DOCUMENTER → документация
```

### Правило 3: Если запрос неясный

```
Ты: "надо форму"  [очень кратко]
Я:  PROMPT-OPTIMIZER → задаёт уточнения
    ↓
    [ты отвечаешь]
    ↓
    Формирует детальный промт
    ↓
    Делегирует UI-DESIGNER + CODER
```

---

## 📝 Примеры делегирования (детальные сценарии)

### Пример 1: Создание формы

**Твой запрос:**
```
Создай форму для Пояснительной записки
```

**Что я делаю (внутренний процесс):**

**Шаг 1: Анализ**
```
- Запрос понятен
- Нужна форма (UI + код)
- Агенты: UI-DESIGNER → CODER
```

**Шаг 2: Делегирую UI-DESIGNER**
```
🎯 Задача для @UI-DESIGNER:
Спроектируй форму для Пояснительной записки v01.05

📋 Требования:
- 12 разделов (Accordion)
- Поля: текст, textarea, select, repeater
- BEM стили, минималистичный дизайн
- Real-time валидация

📁 Результат:
- Макет (ASCII-арт)
- HTML структура
- CSS (BEM)
```

**Шаг 3: UI-DESIGNER возвращает**
```
[Макет формы с ASCII-артом]
[HTML структура]
[CSS классы]
```

**Шаг 4: Делегирую CODER**
```
🎯 Задача для @CODER:
Реализуй форму на основе макета от UI-DESIGNER

📋 Вход:
- [Макет от UI-DESIGNER]

📋 Требования:
- Vanilla JS (без фреймворков)
- Класс FormRenderer
- Two-way binding
- Автосохранение

📁 Результат:
- src/renderer/js/form-renderer.js
- Пример использования
```

**Шаг 5: CODER возвращает**
```javascript
// Полный код FormRenderer
class FormRenderer {
  constructor(container, schema) { ... }
  render() { ... }
  validate() { ... }
}

// Пример использования
const form = new FormRenderer('#form-container', schema);
form.render();
```

**Шаг 6: Автоматически вызываю REVIEWER**
```
🎯 Задача для @REVIEWER:
Проверь код FormRenderer

📋 Что проверить:
- Соблюдение best practices
- Потенциальные баги
- Performance issues
- Соответствие стилю кода проекта

📁 Результат:
- Список замечаний
- Рекомендации
```

**Шаг 7: REVIEWER возвращает**
```
✅ Код хороший, но есть замечания:
1. Добавить debounce на автосохранение
2. Проверку на null перед querySelector
3. JSDoc комментарии

Предложенные исправления:
[код с исправлениями]
```

**Шаг 8: Финальная версия кода**
```
Я применяю исправления от REVIEWER и выдаю тебе:

✅ Готово! Создана форма для ПЗ v01.05

Файлы:
- src/renderer/js/form-renderer.js (280 строк)
- src/renderer/css/components/form.css (150 строк)
- Пример: examples/form-usage.js

Особенности:
✅ 12 разделов с Accordion
✅ Real-time валидация
✅ Автосохранение (debounce 2 сек)
✅ BEM CSS
✅ JSDoc документация
```

**Время:** 30-60 секунд (вместо 2-3 часов вручную)

---

### Пример 2: Исправление бага

**Твой запрос:**
```
Не работает сохранение, ошибка "Cannot read property 'id' of undefined"
в storage-manager.js:45
```

**Что я делаю:**

**Шаг 1: Вызываю DEBUGGER**
```
🎯 Задача для @DEBUGGER:
Найди и исправь баг

📦 Контекст:
- Файл: storage-manager.js:45
- Ошибка: Cannot read property 'id' of undefined
- Метод: saveDocument()

📋 Действия:
1. Прочитай storage-manager.js
2. Найди строку 45
3. Определи причину (почему 'id' undefined)
4. Предложи исправление
```

**Шаг 2: DEBUGGER анализирует**
```
[Читает файл storage-manager.js]
[Находит строку 45: const docId = document.id]
[Анализирует, откуда приходит document]
[Определяет: document может быть undefined, если...]

🐛 Найдена причина:
В методе saveDocument(data) параметр data может быть undefined,
если вызвать без аргументов.

🔧 Исправление:
Добавить проверку:
if (!data || !data.id) {
  throw new Error('Document data is required');
}
```

**Шаг 3: Я выдаю тебе**
```
🐛 Баг исправлен!

Причина:
Метод saveDocument() вызывается без данных, поэтому data.id → undefined

Исправление (storage-manager.js:43-48):

async saveDocument(data) {
  // ✅ Добавлена проверка
  if (!data || typeof data !== 'object') {
    throw new Error('Document data is required and must be an object');
  }

  const docId = data.id; // Теперь безопасно
  // ... остальной код
}

Также рекомендую:
1. Добавить TypeScript или JSDoc для типов
2. Валидацию входных параметров во всех публичных методах
```

**Время:** 10-20 секунд

---

### Пример 3: Создание модуля (сложная задача)

**Твой запрос:**
```
Создай модуль "Заключение экспертизы v01.03"
```

**Что я делаю:**

**Шаг 1: Вызываю TASK-SPLITTER** (разбивает на подзадачи)
```
🎯 Задача для @TASK-SPLITTER:
Разбей создание модуля на подзадачи

📋 Вход:
Модуль "Заключение экспертизы v01.03"

📁 Результат:
Список подзадач с оценкой времени
```

**TASK-SPLITTER возвращает:**
```
📋 Подзадачи (всего 8):

1. Архитектура модуля (2ч) → @ARCHITECT
2. Скачать/изучить XSD-схему v01.03 (30мин) → @WEB-RESEARCHER
3. Создать JSON Schema (3ч) → @CODER
4. Создать XML маппинг (2ч) → @CODER
5. Создать UI формы (4ч) → @UI-DESIGNER + @CODER
6. Реализовать валидацию (2ч) → @CODER
7. Написать тесты (3ч) → @TESTER
8. Документация (1ч) → @DOCUMENTER

Итого: 17.5 часов работы
С агентами: 2-3 часа календарного времени
```

**Шаг 2-9: Последовательно вызываю агентов**

```
[Подзадача 1] ARCHITECT → проектирует структуру
              ↓
[Подзадача 2] WEB-RESEARCHER → находит XSD-схему
              ↓
[Подзадача 3-4] CODER → создаёт JSON Schema + маппинг
              ↓
[Подзадача 5] UI-DESIGNER → дизайн форм
              ↓
              CODER → реализует формы
              ↓
[Подзадача 6] CODER → валидация
              ↓
[Подзадача 7] TESTER → тесты
              ↓
[Подзадача 8] DOCUMENTER → документация
              ↓
           REVIEWER → финальная проверка всего
```

**Шаг 10: Финальный результат**
```
✅ Модуль "Заключение экспертизы v01.03" готов!

Созданные файлы:
plugins/expert-conclusion-01.03/
├── module.json (манифест)
├── schema.json (JSON Schema, 10 разделов)
├── mapping.json (XML маппинг)
├── ui/
│   ├── form.html (структура формы)
│   ├── form.css (стили, BEM)
│   └── form.js (логика формы)
├── validators/
│   └── business-rules.js (бизнес-правила)
├── tests/
│   ├── schema.test.js (unit тесты)
│   └── form.test.js (E2E тесты)
└── README.md (документация модуля)

Покрытие тестами: 87%
Время разработки: 2.5 часа
Размер: 1,240 строк кода

Готово к использованию! 🚀
```

---

## 🎨 Специальные возможности

### 1. Web Research (автопоиск решений)

**Когда использую:**
- Тебе нужен best practice
- Непонятно, как решить задачу
- Нужна актуальная информация (2024-2025)

**Пример:**
```
Ты: "Как оптимизировать SQLite для Electron?"

Я:  WEB-RESEARCHER → ищет в интернете
    ↓
    Находит статьи/гайды 2024-2025
    ↓
    Анализирует лучшие практики
    ↓
    Возвращает конкретные рекомендации:

📊 Best practices для SQLite в Electron (2024):

1. Используй WAL mode (Write-Ahead Logging):
   ```sql
   PRAGMA journal_mode=WAL;
   ```
   Прирост: +50% к скорости записи

2. Пакетные транзакции:
   BEGIN; ... COMMIT;
   Прирост: +100x для множественных INSERT

3. Prepared statements (защита от SQL injection):
   db.prepare('SELECT * FROM docs WHERE id = ?')

4. Индексы на частых запросах:
   CREATE INDEX idx_docs_date ON documents(created_at);

5. PRAGMA optimize после bulk operations

[Полный код с примерами]
```

### 2. Code Generation (быстрая генерация)

**Когда использую:**
- Нужен типовой код (CRUD, IPC handler, UI component)
- Есть шаблон в `templates/`

**Пример:**
```
Ты: "Создай IPC handler для получения списка документов"

Я:  CODE-GENERATOR → использует шаблон
    ↓
    templates/electron/ipc-handler.js
    ↓
    Подставляет значения:
    - handler name: 'document:list'
    - method: 'getDocuments'
    - params: filters
    ↓
    Готовый код за 2 секунды:

// src/main/handlers/document-handlers.js
ipcMain.handle('document:list', async (event, filters = {}) => {
  try {
    const documents = await storageManager.listDocuments(filters);
    return { success: true, data: documents };
  } catch (error) {
    console.error('Error listing documents:', error);
    return { success: false, error: error.message };
  }
});
```

**Скорость:** В 5-10 раз быстрее, чем писать вручную.

### 3. Auto Testing (автогенерация тестов)

**Когда использую:**
- Написан новый код
- Нужно покрытие тестами

**Пример:**
```
Ты: "Напиши тесты для FormRenderer"

Я:  AUTO-TESTER → читает код FormRenderer
    ↓
    Анализирует публичные методы
    ↓
    Генерирует тесты:

// tests/form-renderer.test.js
describe('FormRenderer', () => {
  describe('constructor', () => {
    it('should create instance with valid params', () => { ... });
    it('should throw error if container is null', () => { ... });
  });

  describe('render()', () => {
    it('should render form from schema', () => { ... });
    it('should apply BEM classes', () => { ... });
    it('should attach event listeners', () => { ... });
  });

  describe('validate()', () => {
    it('should validate required fields', () => { ... });
    it('should validate regex patterns', () => { ... });
    it('should return errors array', () => { ... });
  });

  // Edge cases
  it('should handle empty schema', () => { ... });
  it('should handle malformed data', () => { ... });
});

// 24 теста, покрытие: 92%
```

### 4. Ollama Consultant (второе мнение)

**Когда использую:**
- Сложное архитектурное решение
- Сомнения в качестве кода
- Нужна альтернативная точка зрения

**Пример:**
```
Ты: "Проверь архитектуру плагинной системы"

Я:  ARCHITECTURE-VALIDATOR → проверяет код
    ↓
    [Находит 3 потенциальных проблемы]
    ↓
    OLLAMA-CONSULTANT → запрашиваю второе мнение
    ↓
    Локальная LLM (DeepSeek Coder) анализирует
    ↓
    Возвращает независимую оценку:

🤖 Второе мнение от Ollama (DeepSeek Coder V2):

✅ Сильные стороны:
- Хорошая изоляция плагинов
- Чёткий API контракт
- Песочница для безопасности

⚠️ Потенциальные проблемы:
1. Отсутствует версионирование API плагинов
   → Решение: Добавить semver для plugin.version

2. Нет обработки циклических зависимостей
   → Решение: Topological sort при загрузке

3. Memory leak при перезагрузке плагинов
   → Решение: Явная очистка listeners в unload()

📊 Качество кода: 8.5/10
Рекомендация: Внести 3 исправления выше

[Исправленный код с комментариями]
```

**Преимущество:** Двойная проверка = качество кода +20-30%

---

## 🔧 Как я оптимизирую работу

### Автоматизация рутины

**Что я делаю автоматически** (без твоего участия):

1. **Код-ревью** — после каждого написанного кода вызываю REVIEWER
2. **Валидация архитектуры** — проверяю соблюдение SOLID, DRY, KISS
3. **Оптимизация промтов** — улучшаю твои короткие запросы через PROMPT-OPTIMIZER
4. **Разбивка задач** — большие задачи разбиваю через TASK-SPLITTER
5. **Генерация тестов** — для нового кода создаю unit-тесты
6. **Документация** — добавляю JSDoc комментарии в код

**Ты просто пишешь задачу** → Я делаю всё остальное.

### Параллельная работа

Когда возможно, **запускаю агентов параллельно**:

**Пример:**
```
Ты: "Создай модуль ПЗ v01.05"

Я запускаю одновременно:
┌─────────────────┬──────────────────┬───────────────────┐
│ WEB-RESEARCHER  │ ARCHITECT        │ CODE-GENERATOR    │
│ (ищет XSD)      │ (проектирует)    │ (каркас модуля)   │
└─────────────────┴──────────────────┴───────────────────┘
         ↓                  ↓                  ↓
      [2 мин]           [3 мин]            [1 мин]
         └──────────────────┴────────────────┘
                           ↓
                    [Объединяю результаты]
                           ↓
                       UI-DESIGNER
                       (дизайн форм)
                           ↓
                        [5 мин]
                           ↓
                         CODER
                      (реализация)
```

**Время:** 11 минут вместо 20 (если бы последовательно)

### Кэширование знаний

Я **запоминаю контекст проекта**:
- Структуру файлов
- Используемые библиотеки
- Стиль кода
- Архитектурные решения

**Пример:**
```
Ты: "Создай ещё один IPC handler"

Я (уже знаю):
- Где лежат handlers (src/main/handlers/)
- Какой паттерн используется
- Какие библиотеки подключены
- Стиль кода проекта

→ Сразу генерирую код (без уточнений)
```

---

## 📋 Типичные сценарии работы

### Сценарий 1: Начало разработки (Phase 1, Week 1)

**Твой запрос:**
```
Начинаем Week 1: Infrastructure Setup
```

**Что я делаю:**

**Шаг 1:** Читаю roadmap (из `concept/SOLO_DEV_ROADMAP.md`)
```
Week 1 задачи:
1. Electron project setup
2. SQLite integration
3. IPC handlers
4. Basic UI structure
```

**Шаг 2:** Вызываю TASK-SPLITTER
```
Разбивает на 12 подзадач с зависимостями
```

**Шаг 3:** Последовательно делегирую агентам
```
1. ARCHITECT → структура проекта
2. CODE-GENERATOR → electron boilerplate
3. CODER → SQLite StorageManager
4. CODER → IPC handlers (preload bridge)
5. UI-DESIGNER → base HTML/CSS
6. TESTER → setup testing (Mocha/Jest)
7. DOCUMENTER → README.md
```

**Шаг 4:** Проверки
```
ARCHITECTURE-VALIDATOR → проверяет структуру
REVIEWER → ревью всего кода
OLLAMA-CONSULTANT → второе мнение
```

**Результат через 1-2 часа:**
```
✅ Week 1 завершена!

Создано:
- 18 файлов
- 2,400 строк кода
- 15 unit-тестов
- Полная документация

Готово к Week 2!
```

### Сценарий 2: Ежедневная разработка

**Типичный день (твои запросы):**

**Утро (9:00-12:00):**
```
09:00 Ты: "Создай JSON Schema для раздела 3 ПЗ (ТЭП)"
      Я:  CODER → создаёт schema
          AUTO-TESTER → тесты
          [15 минут]

10:00 Ты: "Добавь валидацию площадей (не может быть отрицательной)"
      Я:  CODER → добавляет валидацию
          TESTER → тест для edge case
          [5 минут]

11:00 Ты: "Оптимизируй запросы к базе, медленно работает"
      Я:  PERFORMANCE → анализирует
          WEB-RESEARCHER → ищет best practices
          CODER → добавляет индексы + prepared statements
          [20 минут]
```

**День (13:00-18:00):**
```
13:00 Ты: "Создай форму для раздела 3"
      Я:  UI-DESIGNER → макет
          CODER → реализация
          REVIEWER → проверка
          [40 минут]

15:00 Ты: "Баг: форма не сохраняет данные"
      Я:  DEBUGGER → находит причину
          CODER → фикс
          [10 минут]

17:00 Ты: "Код-ревью сегодняшнего кода"
      Я:  REVIEWER → проверяет весь код
          OLLAMA-CONSULTANT → второе мнение
          REFACTORER → улучшения
          [15 минут]
```

**Итого за день:**
- 6 задач выполнено
- 1,200 строк кода написано
- 0 багов в продакшене (благодаря двойной проверке)
- Время твоей работы: 1.5 часа (остальное агенты)

### Сценарий 3: Код-ревью и рефакторинг (раз в неделю)

**Пятница (конец недели):**
```
Ты: "Провести код-ревью всего написанного за неделю"

Я:
    1. REVIEWER → проверяет весь код
    2. ARCHITECTURE-VALIDATOR → проверяет архитектуру
    3. PERFORMANCE → профилирует (ищет bottlenecks)
    4. SECURITY → аудит безопасности
    5. OLLAMA-CONSULTANT → независимая оценка

    Результат:

    📊 Отчёт за неделю:

    ✅ Код качественный:
    - Соблюдены best practices: 95%
    - Покрытие тестами: 87%
    - Производительность: отлично
    - Безопасность: критичных проблем нет

    ⚠️ Найдено 8 улучшений:
    1. FormManager: добавить memoization (PERFORMANCE)
    2. storage-manager.js:45 — потенциальный memory leak (REVIEWER)
    3. Missing JSDoc в 3 методах (DOCUMENTER)
    4. Можно упростить validate() (REFACTORER)
    ...

    📁 Исправления:
    [Код с исправлениями для каждого пункта]

    Применить? [Да / Выборочно / Отложить]
```

---

## 🎯 Как эффективно делегировать

### Правило "одна задача — один запрос"

✅ **Хорошо:**
```
"Создай FormManager"
[жду результат]
"Теперь добавь валидацию"
[жду результат]
"Оптимизируй рендеринг"
```

❌ **Плохо:**
```
"Создай FormManager, добавь валидацию, оптимизируй рендеринг,
напиши тесты, сделай документацию"
```

**Почему:** Я могу сделать всё одновременно, но результат будет менее качественный (слишком много в одном запросе).

### Правило "доверяй агентам"

Не пиши детали реализации, **если это типовая задача**:

✅ **Хорошо:**
```
"Создай CRUD для документов"
```

❌ **Излишне:**
```
"Создай CRUD для документов: нужен класс DocumentManager,
методы create, read, update, delete, каждый должен принимать id,
возвращать Promise, использовать async/await..."
```

**Почему:** Агент CODER знает, как делать CRUD. Он сам добавит всё необходимое.

**Исключение:** Если есть **специфичные требования** проекта:
```
"Создай CRUD для документов, но вместо Promise используй callback
для совместимости со старой частью кода"
```

### Правило "уточняй, когда PROMPT-OPTIMIZER спрашивает"

Если PROMPT-OPTIMIZER задал вопросы — **ответь на них**:

```
PROMPT-OPTIMIZER:
🤔 Уточнения:
- Для какого типа документа?
- Какие поля обязательны?
- Нужна ли валидация в реальном времени?

Ты: [отвечаешь]

PROMPT-OPTIMIZER:
✅ Понял, делегирую CODER + UI-DESIGNER
```

Не игнорируй вопросы, иначе результат может быть не тем, что ожидаешь.

---

## 🚀 Продвинутые техники

### 1. Цепочки агентов (Pipelines)

Для сложных задач создаю **цепочки**:

**Пример: "Создай + протестируй + задокументируй модуль"**

```
Pipeline:
ARCHITECT → проектирует
    ↓
CODE-GENERATOR → создаёт каркас
    ↓
CODER → реализует логику
    ↓
REVIEWER → проверяет код
    ↓ (если есть замечания)
CODER → исправляет
    ↓
AUTO-TESTER → генерирует тесты
    ↓
TESTER → проверяет покрытие (должно быть > 80%)
    ↓ (если < 80%)
AUTO-TESTER → дописывает тесты
    ↓
DOCUMENTER → создаёт документацию
    ↓
OLLAMA-CONSULTANT → финальная проверка
    ↓
[Результат тебе]
```

**Ты пишешь 1 запрос** → Я запускаю 7-10 агентов автоматически.

### 2. Контекстное обучение

Чем больше мы работаем, **тем умнее становятся агенты**:

**Week 1:**
```
Ты: "Создай IPC handler"
Я:  [Уточняю детали, генерирую код]
```

**Week 5 (я уже знаю стиль проекта):**
```
Ты: "Создай IPC handler для templates"
Я:  [Сразу генерирую код в стиле проекта, без уточнений]
```

### 3. Проактивные агенты

Некоторые агенты работают **проактивно** (сами, без запроса):

**AUTO-VALIDATOR** — проверяет каждый коммит:
```
Ты делаешь git commit

AUTO-VALIDATOR автоматически:
1. Проверяет код на ошибки
2. Запускает тесты
3. Проверяет линтеры
4. Если что-то не так — предупреждает

Пример:
⚠️ Внимание! В коммите найдены проблемы:
- ESLint: 3 ошибки в form-manager.js
- Тесты: 2 теста failed
- Coverage упал с 87% до 82%

Исправить перед коммитом? [Да / Посмотреть / Игнорировать]
```

**PERFORMANCE-MONITOR** — следит за производительностью:
```
После каждого изменения кода измеряет:
- Время рендеринга формы
- Время валидации
- Время сохранения в БД

Если что-то замедлилось > 20%:
⚠️ Performance regression detected!
FormRenderer.render() стал медленнее на 35%
Было: 120ms → Стало: 162ms

Причина: [анализ]
Исправление: [код]

Применить? [Да / Посмотреть / Отложить]
```

---

## 📊 Метрики эффективности

### Сравнение: Без агентов vs С агентами

| Задача | Без агентов | С агентами | Ускорение |
|--------|-------------|------------|-----------|
| Создать CRUD | 2 часа | 15 минут | **8x** |
| Дизайн формы | 3 часа | 25 минут | **7x** |
| Написать тесты | 1.5 часа | 10 минут | **9x** |
| Исправить баг | 30 минут | 5 минут | **6x** |
| Создать модуль | 2 дня | 3 часа | **5x** |
| Код-ревью | 1 час | 10 минут | **6x** |

**Средний прирост:** **~7x быстрее**

### Качество кода

| Метрика | Без агентов | С агентами |
|---------|-------------|------------|
| Покрытие тестами | 40-60% | 85-95% |
| Соблюдение best practices | 60-70% | 90-98% |
| Найденных багов в продакшене | 5-10/неделю | 0-2/неделю |
| Время на исправление бага | 1-2 часа | 10-30 минут |

**Вывод:** Качество кода **+30-40%** благодаря двойной проверке (Claude + Ollama).

---

## ❓ FAQ

### В: Должен ли я вручную вызывать агентов?

**О:** Нет! Я делаю это автоматически. Просто пиши задачу.

**Исключение:** Если хочешь явно указать агента:
```
"@DEBUGGER найди баг в storage-manager.js"
```
Но обычно это не нужно — я сам определю.

---

### В: Что если агент сделал не то, что я хотел?

**О:** Напиши уточнение:
```
Ты: "Не то, нужно вот так: [описание]"
Я:  Понял, исправляю...
    [Делегирую CODER с новыми требованиями]
```

Или:
```
Ты: "Откати изменения, давай по-другому"
Я:  Откатил. Как нужно?
```

---

### В: Могу ли я работать с несколькими агентами одновременно?

**О:** Да, я сам запускаю агентов параллельно, когда это возможно.

Если хочешь явно:
```
"Создай форму (UI-DESIGNER + CODER) и одновременно
оптимизируй запросы к БД (PERFORMANCE)"
```

Я запущу 3 агента параллельно.

---

### В: Как узнать, какой агент сейчас работает?

**О:** Я сообщаю в процессе:
```
🎯 Делегирую @UI-DESIGNER: проектирование формы...
✅ @UI-DESIGNER завершил работу

🎯 Делегирую @CODER: реализация формы...
✅ @CODER завершил работу

🎯 Делегирую @REVIEWER: проверка кода...
✅ @REVIEWER завершил работу

Результат готов!
```

---

### В: Что делать, если задача очень сложная?

**О:** Опиши высокоуровнево, я вызову TASK-SPLITTER:

```
Ты: "Создай полноценный модуль Экспертизы"

Я:  TASK-SPLITTER разобьёт на подзадачи
    ↓
    Покажу тебе список (например, 15 подзадач)
    ↓
    Спрошу: Начинать? [Да / Изменить приоритет / Отложить]
    ↓
    Последовательно выполняю через агентов
```

---

## ✅ Чек-лист для эффективной работы

Перед тем как писать запрос:

- [ ] Задача конкретная? (не "доделай проект", а "создай форму ПЗ")
- [ ] Понятно, что нужно? (если нет — PROMPT-OPTIMIZER уточнит)
- [ ] Задача разумного размера? (если большая — TASK-SPLITTER разобьёт)
- [ ] Доверяешь агентам? (не пиши реализацию, если это типовая задача)

Если все пункты ✅ — пиши запрос, я сделаю всё остальное!

---

## 🎯 Резюме

### Главные правила работы со мной:

1. **Пиши короткие, конкретные запросы** → Я делегирую агентам
2. **Не вызывай агентов вручную** → Я сам определю нужных
3. **Отвечай на уточнения PROMPT-OPTIMIZER** → Это улучшит результат
4. **Доверяй агентам** → Они эксперты в своих областях
5. **Проверяй результат** → Если не то — скажи, исправлю

### Что я делаю автоматически:

✅ Оптимизирую твои запросы (PROMPT-OPTIMIZER)
✅ Выбираю нужных агентов
✅ Делегирую задачи
✅ Проверяю код (REVIEWER + OLLAMA-CONSULTANT)
✅ Генерирую тесты (AUTO-TESTER)
✅ Пишу документацию (DOCUMENTER)
✅ Валидирую архитектуру (ARCHITECTURE-VALIDATOR)
✅ Ищу оптимизации (PERFORMANCE)

### Твоя работа:

1. Пишешь задачи (короткие запросы)
2. Отвечаешь на уточнения (если нужно)
3. Проверяешь результат
4. Тестируешь в приложении

**Соотношение:** Ты — 10-20% времени, Агенты — 80-90% времени.

---

**Готов к работе! Пиши первую задачу 🚀**