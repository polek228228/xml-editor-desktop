# Рабочий процесс разработки XML Editor Desktop

## Стратегия делегирования субагентов

### Когда использовать специализированных агентов:

#### 🖥️ Main Process Issues → `/agent main-process-agent.md`
- Проблемы с жизненным циклом приложения
- Настройка BrowserWindow и безопасность
- IPC обработчики и коммуникация
- SQLite операции через StorageManager
- Файловые операции и нативные API

#### 🌐 UI/Renderer Issues → `/agent renderer-agent.md`
- FormManager и создание форм
- UI компоненты (Accordion, InputField)
- DOM манипуляции и события
- Клиентская валидация XML
- CSS стили и адаптивность

#### 💾 Database Problems → `/agent database-agent.md`
- Схема базы данных и миграции
- Оптимизация запросов
- Автосохранение и история документов
- Резервное копирование данных

## Эффективные промты для работы с проектом

### Для анализа ошибок:
```
"Анализируй ошибку в [компонент] - [описание проблемы]. Проверь [конкретные файлы] и предложи решение с учетом архитектуры Electron и безопасности"
```

### Для добавления функционала:
```
"Реализуй [функция] в [компонент]. Следуй архитектуре проекта: IPC для main-renderer коммуникации, BEM для CSS, валидация для XML схем Минстроя"
```

### Для отладки:
```
"Отладь [проблему] используя /debug-electron. Проверь процессы, логи, IPC каналы и базу данных. Приоритет: безопасность Electron"
```

## Приоритеты разработки:

1. **Безопасность Electron** - всегда первый приоритет
2. **Соответствие схемам Минстроя** - критично для продукта
3. **Производительность** - особенно для больших XML документов
4. **UX для русских пользователей** - локализация и удобство

## Мониторинг производительности:
- Main process memory: `process.memoryUsage()`
- Renderer performance: Chrome DevTools
- Database queries: SQLite EXPLAIN QUERY PLAN
- IPC latency: Custom timing middleware