# 🤖 ДЛЯ КЛАУДА ПРИ ПЕРВОМ СТАРТЕ

**Последнее обновление:** 6 октября 2025
**Версия:** 2.1
**Статус проекта:** Week 3 завершена, переходим к Week 4

---

## 🎯 ЧТО ЗА ПРОЕКТ (30 секунд)

**XML Editor Desktop** — Electron-приложение для создания XML пояснительных записок по стандартам Минстроя РФ (клон xmlonline.ru, но offline).

**Ключевые факты:**
- 🖥️ **Стек:** Electron 27, SQLite, Vanilla JS, BEM CSS
- 📅 **План:** 12 недель разработки (3 фазы)
- 📊 **Прогресс:** Week 3 из 12 завершена (~25%)
- 💰 **Бизнес-модель:** Модульные продажи (5,990₽ за ПЗ v01.05)
- 👥 **Целевая аудитория:** 67,000 проектных организаций

---

## ✅ ЧТО УЖЕ СДЕЛАНО (Week 1-3)

### Week 1: Infrastructure ✅
- Electron app setup (main/renderer/preload)
- SQLite database (6 таблиц: documents, templates, autosaves, settings, document_history, modules)
- IPC communication bridge (14 handlers)
- Security: contextIsolation, sandbox enabled

### Week 2: Form System & XML Generation ✅
- FormManager — динамические формы из JSON schema
- SchemaLoader — загрузка схем ПЗ 01.03/01.04/01.05
- Validators — ИНН, ОГРН, email, телефон, даты
- RichTextEditor — TinyMCE integration
- XMLGenerator — экспорт в XML Минстроя
- DocumentSelector — modal для выбора документов

### Week 3: 3-Level UI Architecture ✅
- **App Nav** (4 глобальных раздела): Главная, Документы, Сервисы, Настройки
- **Dynamic Sidebar** (категории + поиск + фильтры)
- **Main Content** (Home Dashboard, Service Store, Editor, Settings)
- **Context Toolbar** (условная панель при открытом документе)
- Service Store UI (магазин модулей с карточками, badges, rating)
- E2E Tests (64 теста, 87.5% pass rate)

**Итого:**
- 📝 ~12,000 строк кода (src/)
- 📄 ~5,000 строк документации
- 🧪 64 E2E теста
- 🤖 17 AI-агентов настроены

---

## ❌ ИЗВЕСТНЫЕ БАГИ (из последней сессии)

### Критичные (P0):
1. **Ошибка `this.switchTab is not a function`** (app.js:1195)
   - При создании документа из шаблона
   - Метод `switchTab()` не существует в классе XMLEditorApp

2. **Шаблоны не отображаются после создания**
   - Создаешь шаблон → UI не обновляется
   - Нужно перезапускать приложение

3. **Документ не открывается после создания из шаблона**
   - Показывает уведомление "Документ создан", но UI не переключается в редактор
   - Связано с багом #1

### Средние (P1):
4. **Баг выхода из меню шаблонов**
   - Закрываешь TemplateBrowser → UI не возвращается в предыдущий экран
   - Приходится перезапускать

5. **Отсутствие визуального feedback при сохранении**
   - Нажимаешь "Сохранить" → нет индикации прогресса
   - Только toast notification (быстро пропадает)

---

## 🔍 ТЕКУЩЕЕ СОСТОЯНИЕ КОДА (6 октября 2025)

### Файловая структура (важные файлы):

```
xmlPZ/
├── src/
│   ├── main/
│   │   ├── main.js (1,200 строк) — Main process + IPC handlers
│   │   ├── storage-manager.js (650 строк) — SQLite operations
│   │   └── template-manager.js (400 строк) — Template CRUD
│   ├── renderer/
│   │   ├── index.html (467 строк) — 3-level UI structure
│   │   ├── css/main.css (2,448 строк) — BEM styles
│   │   └── js/
│   │       ├── app.js (1,163 строк) — XMLEditorApp class ⚠️ СОДЕРЖИТ БАГИ
│   │       ├── form-manager.js (560 строк) — Form rendering/validation
│   │       ├── schema-loader.js (280 строк) — JSON schema loading
│   │       ├── validators.js (420 строк) — Input validation
│   │       ├── xml-generator.js (380 строк) — XML export
│   │       └── components/
│   │           ├── accordion.js — Collapsible sections
│   │           ├── input-field.js — Smart input wrapper
│   │           ├── template-dialog.js — Template creation modal
│   │           ├── template-browser.js — Template selection UI
│   │           ├── tab-bar.js — Multi-tab system (Week 3)
│   │           ├── dynamic-sidebar.js — Sidebar logic (Week 3)
│   │           └── service-store.js — Service marketplace (Week 3)
│   ├── preload/
│   │   └── preload.js (180 строк) — IPC bridge (contextBridge)
│   └── schemas/
│       ├── json/ — JSON schemas для форм (pz-01.03/01.04/01.05)
│       ├── xsd/ — XSD schemas Минстроя (pz-01.05.xsd)
│       └── mappings/ — JSON→XML mappings
│
├── docs/
│   ├── project/FINAL_PROJECT_CONCEPT.md — Главная концепция проекта
│   ├── development/РУКОВОДСТВО_ПО_РАБОТЕ_С_CLAUDE.md — Как работать с агентами
│   ├── for-client/КОНЦЕПЦИЯ_ДЛЯ_ЗАКАЗЧИКА.md — Презентация для клиента
│   ├── UI_ARCHITECTURE.md — Документация 3-level архитектуры
│   └── archive/ — Старые анализы и отчеты
│
├── agents/ (17 AI-агентов)
│   ├── AGENTS_INDEX.md — Список всех агентов
│   ├── core/ — ARCHITECT, CODER, REVIEWER, DEBUGGER, REFACTORER
│   ├── design/ — UI-DESIGNER v2.1, UX-ANALYST v2.1
│   ├── quality/ — TESTER, PERFORMANCE, SECURITY
│   ├── content/ — DOCUMENTER
│   ├── helpers/ — PROMPT-OPTIMIZER ⭐, TASK-SPLITTER
│   └── advanced/ — WEB-RESEARCHER, CODE-GENERATOR, AUTO-TESTER
│
├── e2e/ (64 E2E теста)
│   ├── 00-smoke.e2e.js — Smoke tests (9 tests) ✅
│   ├── 01-documents.e2e.js — Document CRUD (11 tests)
│   ├── 02-xml-generation.e2e.js — XML export (10 tests)
│   ├── 03-validation-templates.e2e.js — Validation + templates (13 tests)
│   ├── 04-ui-navigation.e2e.js — 3-level nav (18 tests)
│   └── 99-full-scenario.e2e.js — Full user flows (3 tests)
│
├── CLAUDE.md — Правила кода для Claude
├── ИНСТРУКЦИЯ_ДЛЯ_CLAUDE.md — Главная инструкция (прочитай ПЕРВЫМ!)
├── Для Клауда при первом старте.md — Этот файл
├── KNOWN_ISSUES.md — Известные баги (обновлен 6 октября)
├── UI_UX_UPDATE_REPORT.md — Отчет о Week 3
├── E2E_TEST_REPORT.md — Отчет о тестировании
└── package.json
```

### Ключевые методы XMLEditorApp (app.js):

**Работают:**
- ✅ `init()` — Инициализация
- ✅ `createNewDocument()` — Создание документа
- ✅ `saveDocument()` — Сохранение в БД
- ✅ `openDocument()` — Открытие существующего
- ✅ `exportXML()` — Экспорт в XML
- ✅ `validateXML()` — Валидация по XSD
- ✅ `showToast()` — Toast notifications
- ✅ `cleanupUI()` — Удаление modal overlays

**Сломаны/Отсутствуют:**
- ❌ `switchTab()` — Метод не существует! (вызывается в строке 1181)
- ❌ `showEditorScreen()` — Не всегда переключает UI корректно
- ⚠️ `handleTemplateSelect()` — Работает, но не переключает на editor

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ (Week 4: Module System)

### Приоритет P0 (сначала FIX BUGS!):

**1. Исправить критичные баги** (2-3 часа)
   - [ ] Удалить вызов `this.switchTab()` в app.js:1181 (метод не существует)
   - [ ] Исправить `handleTemplateSelect()` — должен открывать editor после создания документа
   - [ ] Исправить TemplateBrowser — корректно закрывать и возвращаться к предыдущему экрану
   - [ ] Добавить UI refresh после создания шаблона

**2. Затем Week 4 задачи** (22 часа):
   - [ ] ModuleRegistry — регистрация модулей в БД
   - [ ] PluginLoader — динамическая загрузка модулей
   - [ ] Service installation flow (купить → установить → активировать)
   - [ ] IPC handlers для модулей (`module:install`, `module:list`, `module:uninstall`)
   - [ ] SQLite таблица `modules` (уже создана)
   - [ ] Service metadata (descriptions, icons, ratings) — загрузка из БД

---

## 📚 КАК РАБОТАТЬ С ПРОЕКТОМ

### 1️⃣ При первом запуске (ВСЕГДА):
```bash
# 1. Прочитай главную инструкцию
cat "ИНСТРУКЦИЯ_ДЛЯ_CLAUDE.md"

# 2. Прочитай правила кода
cat "CLAUDE.md"

# 3. Прочитай этот файл (ты здесь)
cat "Для Клауда при первом старте.md"

# 4. Прочитай концепцию проекта
cat "docs/project/FINAL_PROJECT_CONCEPT.md"

# 5. Готов к работе!
```

### 2️⃣ Запуск приложения:
```bash
cd /Users/PotapovViS/Downloads/Discord-Telegram-Bridge-development/xmlPZ
npm run dev
```

**Ожидаемый результат:**
- Electron окно открывается
- Home Dashboard виден по умолчанию
- 4 App Nav раздела: Главная, Документы, Сервисы, Настройки
- Sidebar с категориями
- Логи в консоли: "Database initialized successfully", "XMLValidator initialized"

### 3️⃣ Тестирование:
```bash
# Smoke tests (9 тестов, ~25 сек)
npm run test:e2e:smoke

# All tests (64 теста, ~10 мин)
npm run test:e2e

# Отчет HTML
npm run test:e2e:report
```

### 4️⃣ Работа с агентами:

**Короткий запрос от разработчика:**
```
Разработчик: "Сделай форму для ПЗ 01.05"
```

**Твоя реакция:**
```
1. Вызываю PROMPT-OPTIMIZER (уточняет детали)
2. PROMPT-OPTIMIZER формирует полный prompt
3. Делегирую задачу:
   - UI-DESIGNER → проектирует макет
   - CODER → реализует код
   - REVIEWER → проверяет
   - AUTO-TESTER → генерирует тесты
4. Выдаю результат разработчику
```

**Список агентов:** См. `agents/AGENTS_INDEX.md`

---

## ⚠️ КРИТИЧНЫЕ ПРАВИЛА (что НЕ делать)

### 🚫 НИКОГДА:
1. **НЕ использовать React/Vue/Angular** → только Vanilla JS
2. **НЕ использовать nodeIntegration: true** → security risk
3. **НЕ писать SQL напрямую в renderer** → только через IPC
4. **НЕ забывать `schema_version`** в templates — критичный баг
5. **НЕ использовать `.all()`, `.get()` на db** → используй `allQuery()`, `getQuery()` (StorageManager)
6. **НЕ перекрывать переменную `document`** → конфликт с DOM

### ✅ ВСЕГДА:
1. **BEM для CSS** — `block__element--modifier`
2. **JSDoc комментарии** — для всех публичных методов
3. **Валидация входных данных** — в каждом методе
4. **Автосохранение каждые 30 сек** — как в xmlonline.ru
5. **История версий** — для всех изменений документа
6. **Использовать агентов** — делегировать задачи
7. **Читать `CLAUDE.md`** перед написанием кода

---

## 🐛 ДЕБАГГИНГ

### Как найти баг:
```bash
# 1. Смотри логи в консоли Electron (npm run dev)
# 2. Проверь IPC handlers в src/main/main.js
# 3. Проверь preload API в src/preload/preload.js
# 4. Используй Chrome DevTools (View → Toggle Developer Tools)
```

### Типичные ошибки:

**"Method X is not a function"**
→ Проверь, что метод определен в классе (например, `switchTab()` не существует в XMLEditorApp)

**"Cannot read property 'id' of undefined"**
→ Проверь, что document.id инициализирован перед использованием

**"IPC channel not found"**
→ Проверь, что handler зарегистрирован в main.js и экспонирован в preload.js

**"Database locked"**
→ Закрой все Electron окна и перезапусти приложение

---

## 📊 ROADMAP (что дальше)

### Phase 1: Infrastructure (Weeks 1-4) — 82 часа
- ✅ Week 1: Electron + SQLite + IPC (ГОТОВО)
- ✅ Week 2: Form System + XML Generation (ГОТОВО)
- ✅ Week 3: 3-Level UI Architecture (ГОТОВО)
- ⏳ Week 4: Module System (В РАБОТЕ — но сначала FIX BUGS!)

### Phase 2: Module 1 - ПЗ v01.05 (Weeks 5-8) — 105 часов
- Week 5: JSON Schema для ПЗ (12 разделов)
- Week 6: UI + TinyMCE integration (расширенная)
- Week 7: PDF генерация (XSLT)
- Week 8: Шаблоны, история версий

### Phase 3: Polish & Release (Weeks 9-12) — 94 часа
- Week 9: Testing (Jest + Playwright расширенные)
- Week 10: UI/UX polishing, горячие клавиши
- Week 11: Build pipeline (electron-builder)
- Week 12: Документация, landing page, запуск

**Текущий прогресс:** Week 3 завершена = 25% проекта

---

## 🎯 БЫСТРЫЕ ОТВЕТЫ

**Q: Какой стек?**
A: Electron 27, SQLite, Vanilla JS, BEM CSS

**Q: Где документация?**
A: `docs/project/FINAL_PROJECT_CONCEPT.md` (главная), `CLAUDE.md` (правила кода)

**Q: Как запустить?**
A: `npm run dev`

**Q: Как тестировать?**
A: `npm run test:e2e`

**Q: Что сейчас сломано?**
A: См. раздел "ИЗВЕСТНЫЕ БАГИ" выше

**Q: С чего начать?**
A: Прочитай `ИНСТРУКЦИЯ_ДЛЯ_CLAUDE.md` → `CLAUDE.md` → этот файл

**Q: Как работать с агентами?**
A: Читай `docs/development/РУКОВОДСТВО_ПО_РАБОТЕ_С_CLAUDE.md`

**Q: Где список агентов?**
A: `agents/AGENTS_INDEX.md`

**Q: Что делать если забыл контекст?**
A: Читай этот файл снова 🙂

---

## 📞 ПОЛЕЗНЫЕ ССЫЛКИ

| Вопрос | Файл |
|--------|------|
| Что за проект? | `docs/project/FINAL_PROJECT_CONCEPT.md` |
| Как работать с агентами? | `docs/development/РУКОВОДСТВО_ПО_РАБОТЕ_С_CLAUDE.md` |
| Правила кода? | `CLAUDE.md` |
| Список агентов? | `agents/AGENTS_INDEX.md` |
| Презентация для клиента? | `docs/for-client/КОНЦЕПЦИЯ_ДЛЯ_ЗАКАЗЧИКА.md` |
| UI Architecture? | `docs/UI_ARCHITECTURE.md` |
| Известные баги? | `KNOWN_ISSUES.md` |
| Отчет о Week 3? | `UI_UX_UPDATE_REPORT.md` |
| E2E тесты? | `E2E_TEST_REPORT.md` |

---

## ✅ ЧЕКЛИСТ ПЕРЕД РАБОТОЙ

Перед тем как начать разработку, убедись:

- [ ] Прочитал `ИНСТРУКЦИЯ_ДЛЯ_CLAUDE.md`
- [ ] Прочитал `CLAUDE.md`
- [ ] Прочитал этот файл (`Для Клауда при первом старте.md`)
- [ ] Понял текущие баги (раздел "ИЗВЕСТНЫЕ БАГИ")
- [ ] Понял, что сейчас Week 4 (Module System), но СНАЧАЛА нужно FIX BUGS
- [ ] Знаю, что запускать: `npm run dev`
- [ ] Знаю, как тестировать: `npm run test:e2e`
- [ ] Готов к работе!

---

## 🔥 SUMMARY (30 секунд для нового Claude)

Ты работаешь над **XML Editor Desktop** — Electron-приложением для создания XML пояснительных записок (клон xmlonline.ru).

**Прогресс:** Week 3 из 12 завершена (~25%).

**Что работает:**
- ✅ Electron app + SQLite + IPC
- ✅ FormManager + Validators + XMLGenerator
- ✅ 3-level UI (App Nav → Sidebar → Content)
- ✅ Service Store UI (магазин модулей)
- ✅ E2E tests (64 теста, 87.5% pass rate)

**Что сломано (FIX FIRST!):**
- ❌ `this.switchTab()` не существует (app.js:1181)
- ❌ Документ не открывается после создания из шаблона
- ❌ Шаблоны не отображаются после создания
- ❌ Баг выхода из меню шаблонов

**Следующие шаги:**
1. **СНАЧАЛА:** Исправить все баги выше (2-3 часа)
2. **ЗАТЕМ:** Week 4 — Module System (ModuleRegistry + PluginLoader)

**Как начать:**
1. Прочитай `ИНСТРУКЦИЯ_ДЛЯ_CLAUDE.md` + `CLAUDE.md`
2. Прочитай этот файл полностью
3. Запусти `npm run dev` и протестируй баги
4. Исправь баги (приоритет P0)
5. Переходи к Week 4

**Главное правило:** Используй агентов (17 AI-агентов в `agents/`), делегируй задачи, не пиши всё сам.

---

**Версия:** 2.1
**Дата обновления:** 6 октября 2025
**Статус:** ✅ Актуально

🔥 **ЭТОТ ФАЙЛ — ТВОЯ ТОЧКА ВХОДА! ВСЕГДА НАЧИНАЙ С НЕГО!**
