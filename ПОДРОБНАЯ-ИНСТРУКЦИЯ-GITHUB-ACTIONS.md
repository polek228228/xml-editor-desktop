# 📖 Подробная инструкция: Сборка Windows .exe через GitHub Actions

## 💰 СТОИМОСТЬ (ВАЖНО!)

### ✅ БЕСПЛАТНО для публичных репозиториев
- **Unlimited** минут в месяц
- Можно собирать сколько угодно
- Никаких ограничений

### ⚠️ Для приватных репозиториев
| План | Бесплатные минуты | Стоимость |
|------|-------------------|-----------|
| GitHub Free | 2000 минут/месяц | БЕСПЛАТНО |
| GitHub Pro | 3000 минут/месяц | $4/месяц |
| GitHub Team | 3000 минут/месяц | $4/месяц за пользователя |

**Одна сборка Windows = ~10 минут**

Значит на GitHub Free можно собрать **200 раз в месяц бесплатно!**

### 🎯 Рекомендация
**Сделайте репозиторий публичным** → получите UNLIMITED бесплатных сборок!

---

## 📋 ШАГ 0: Подготовка (если еще не сделали)

### Проверить что есть git репозиторий

```bash
# В папке xmlPZ выполнить:
git status
```

Если увидите "fatal: not a git repository" - инициализируйте:
```bash
git init
```

### Убедиться что код не запушен на GitHub

```bash
# Проверить remote
git remote -v
```

Если пусто - нужно создать репозиторий на GitHub (см. ШАГ 1)

---

## 🚀 ШАГ 1: Создать репозиторий на GitHub (если еще нет)

### 1.1. Открыть GitHub

Перейти на https://github.com

Войти в аккаунт (или создать если нет)

### 1.2. Создать новый репозиторий

Кликнуть **"+"** в правом верхнем углу → **"New repository"**

### 1.3. Заполнить форму

```
Repository name:       xmlPZ
Description:           XML Editor Desktop for Ministry of Construction schemas
Public / Private:      ✅ Public (для бесплатных сборок!)

☑️ Add README file      (ОТКЛЮЧИТЬ!)
☑️ Add .gitignore       (ОТКЛЮЧИТЬ!)
☑️ Choose a license     (ОТКЛЮЧИТЬ!)
```

**ВАЖНО:** НЕ добавляйте README, .gitignore, license - они у вас уже есть!

Нажать **"Create repository"**

### 1.4. Скопировать команды

GitHub покажет страницу с командами. Скопируйте URL репозитория:

```
https://github.com/ВАШ_ЮЗЕРНЕЙМ/xmlPZ.git
```

---

## 📤 ШАГ 2: Запушить код на GitHub

### 2.1. Открыть Terminal в папке xmlPZ

```bash
cd /Users/PotapovViS/Downloads/Discord-Telegram-Bridge-development/xmlPZ
```

### 2.2. Добавить remote (если еще нет)

```bash
# Заменить ВАШ_ЮЗЕРНЕЙМ на ваш GitHub username
git remote add origin https://github.com/ВАШ_ЮЗЕРНЕЙМ/xmlPZ.git
```

### 2.3. Добавить все файлы

```bash
# Добавить GitHub Actions workflow
git add .github/workflows/build-windows.yml

# Добавить инструкции
git add GITHUB_ACTIONS_GUIDE.md
git add ПОДРОБНАЯ-ИНСТРУКЦИЯ-GITHUB-ACTIONS.md
git add ГОТОВО-ИНСТРУКЦИЯ.txt

# Добавить остальные файлы (если еще не добавлены)
git add .
```

### 2.4. Создать commit

```bash
git commit -m "feat: Add GitHub Actions for Windows installer build"
```

### 2.5. Запушить на GitHub

```bash
# Первый push
git push -u origin main
```

**Если попросит авторизацию:**
- Username: ваш GitHub username
- Password: **Personal Access Token** (НЕ пароль!)

#### Как создать Personal Access Token:
1. GitHub → Settings (правый верхний угол)
2. Developer settings (внизу слева)
3. Personal access tokens → Tokens (classic)
4. Generate new token → Generate new token (classic)
5. Note: "xmlPZ deploy"
6. Expiration: 90 days (или No expiration)
7. Выбрать scopes:
   - ✅ repo (все подпункты)
   - ✅ workflow
8. Generate token
9. **СКОПИРОВАТЬ ТОКЕН** (больше не покажут!)
10. Использовать его как пароль при git push

---

## 🎮 ШАГ 3: Запустить сборку (РУЧНОЙ ЗАПУСК)

### 3.1. Открыть GitHub репозиторий

```
https://github.com/ВАШ_ЮЗЕРНЕЙМ/xmlPZ
```

### 3.2. Перейти на вкладку Actions

Кликнуть **"Actions"** (между "Pull requests" и "Projects")

### 3.3. Включить Actions (если первый раз)

Если увидите сообщение "Workflows aren't being run on this repository":

Нажать **"I understand my workflows, go ahead and enable them"**

### 3.4. Выбрать workflow

Слева увидите список workflows:
- **"Build Windows Installer"** ← ЭТОТ ВЫБРАТЬ

Кликнуть на него

### 3.5. Запустить сборку

Справа увидите кнопку **"Run workflow"**

Кликнуть → откроется dropdown

В dropdown снова кликнуть зеленую кнопку **"Run workflow"**

### 3.6. Подождать 5-10 минут

Страница обновится, появится запущенный workflow:

```
Build Windows Installer #1
🟡 In progress  (желтый кружок = идет сборка)
```

**Не закрывайте страницу!** Можно кликнуть на название чтобы смотреть логи в реальном времени.

### 3.7. Дождаться завершения

Через 5-10 минут статус изменится:

```
Build Windows Installer #1
✅ Success  (зеленая галочка = готово!)
```

Или:

```
Build Windows Installer #1
❌ Failed  (красный крестик = ошибка)
```

Если ошибка - см. раздел "ПРОБЛЕМЫ И РЕШЕНИЯ" ниже.

---

## 📥 ШАГ 4: Скачать готовый .exe

### 4.1. Кликнуть на завершенную сборку

Кликнуть на название **"Build Windows Installer #1"**

### 4.2. Прокрутить вниз до секции Artifacts

Будет секция **"Artifacts"** с файлами:

```
📦 windows-installer
   Uploaded 5 minutes ago · Expires in 30 days
```

### 4.3. Скачать архив

Кликнуть на **"windows-installer"** → скачается ZIP файл

### 4.4. Распаковать и проверить

Внутри ZIP будет:

```
windows-installer.zip
├── XMLEditorDesktop-Setup-1.0.0.exe        (~110-150 MB)
├── XMLEditorDesktop-Setup-1.0.0.exe.blockmap
└── latest.yml
```

**Главный файл:** `XMLEditorDesktop-Setup-1.0.0.exe`

---

## 🏷️ БОНУС: Автоматическая сборка при релизе

### Как работает

Вместо ручного запуска можно создать **тег версии** → GitHub **автоматически**:
1. Соберет .exe
2. Создаст Release
3. Прикрепит .exe к релизу

### Инструкция

```bash
# Создать тег
git tag v1.0.0

# Запушить тег
git push origin v1.0.0
```

GitHub автоматически запустит сборку!

### Скачать .exe из Release

1. GitHub → вкладка **"Releases"**
2. Кликнуть на **"v1.0.0"**
3. Внизу секция **"Assets"**
4. Скачать **"XMLEditorDesktop-Setup-1.0.0.exe"**

### Создать новую версию

```bash
# Внести изменения в код
git add .
git commit -m "fix: Some bug fix"
git push

# Создать новый тег
git tag v1.0.1
git push origin v1.0.1
```

→ GitHub соберет новую версию v1.0.1 автоматически!

---

## 🐛 ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема: Actions не работает / нет кнопки "Run workflow"

**Причина:** Actions отключены в настройках

**Решение:**
1. GitHub → репозиторий → Settings
2. Слева → Actions → General
3. В секции "Actions permissions" выбрать:
   **"Allow all actions and reusable workflows"**
4. Save

---

### Проблема: Сборка падает с ошибкой "npm ci failed"

**Причина:** Проблемы с зависимостями

**Решение:**
1. Проверить что package.json и package-lock.json есть в репозитории:
   ```bash
   git add package.json package-lock.json
   git commit -m "fix: Add lock file"
   git push
   ```
2. Запустить сборку снова

---

### Проблема: Сборка падает с ошибкой "electron-builder failed"

**Причина:** Проблемы с конфигурацией electron-builder

**Решение:**
1. Проверить что в package.json есть секция "build":
   ```bash
   cat package.json | grep -A 20 '"build"'
   ```
2. Должно быть:
   ```json
   "build": {
     "appId": "com.xmleditor.desktop",
     "win": {
       "target": ["nsis"]
     }
   }
   ```

---

### Проблема: .exe файл не появился в Artifacts

**Причина:** Сборка упала до загрузки артефактов

**Решение:**
1. Открыть логи сборки
2. Найти где ошибка (красные строки)
3. Исправить в коде
4. Запушить снова

---

### Проблема: .exe не запускается на Windows

**Причина 1:** Антивирус блокирует

**Решение:** Отключить антивирус или добавить в исключения

**Причина 2:** Не хватает прав

**Решение:** Запустить от имени администратора

**Причина 3:** Поврежденный файл

**Решение:** Скачать заново

---

### Проблема: "Превышен лимит минут"

**Причина:** Закончились бесплатные минуты (только для приватных репозиториев)

**Решение:**
1. Подождать до следующего месяца
2. Или сделать репозиторий публичным:
   ```
   Settings → Danger Zone → Change visibility → Make public
   ```
3. Или купить GitHub Pro ($4/мес)

---

## 📊 Как посмотреть сколько минут использовано

1. GitHub → Settings (профиль, не репозиторий)
2. Слева → Billing and plans
3. Секция "Actions & Packages"
4. Увидите:
   ```
   Minutes used: 150 / 2000
   ```

---

## 🎯 ИТОГОВЫЙ ЧЕКЛИСТ

- [ ] Создать GitHub репозиторий (публичный!)
- [ ] Запушить код с workflow файлом
- [ ] Перейти на вкладку Actions
- [ ] Запустить "Build Windows Installer"
- [ ] Подождать 5-10 минут
- [ ] Скачать windows-installer.zip из Artifacts
- [ ] Распаковать и получить .exe
- [ ] Отправить пользователю!

---

## 💡 СОВЕТЫ

### 1. Делайте репозиторий публичным
**Зачем:** Unlimited бесплатных минут

**Как:**
- При создании выбрать "Public"
- Или изменить потом: Settings → Change visibility

### 2. Используйте теги для версий
**Зачем:** Автоматические релизы

**Как:**
```bash
git tag v1.0.0
git push origin v1.0.0
```

### 3. Смотрите логи при ошибках
**Зачем:** Понять где проблема

**Как:**
- Actions → кликнуть на failed запуск
- Развернуть красные шаги
- Прочитать ошибку

### 4. Кэшируйте node_modules
**Зачем:** Ускорить сборку с 10 до 5 минут

**Уже настроено в workflow:**
```yaml
- uses: actions/setup-node@v4
  with:
    cache: 'npm'  ← ЭТО
```

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

- GitHub Actions документация: https://docs.github.com/en/actions
- electron-builder документация: https://www.electron.build/
- Мой репозиторий: https://github.com/ВАШ_ЮЗЕРНЕЙМ/xmlPZ
- GitHub Pricing: https://github.com/pricing

---

## ✅ РЕЗЮМЕ

**Что нужно сделать:**
1. Создать GitHub репозиторий (публичный = бесплатно)
2. Запушить код
3. Actions → Run workflow
4. Подождать 10 минут
5. Скачать .exe

**Стоимость:**
- Публичный репозиторий: **БЕСПЛАТНО навсегда**
- Приватный репозиторий: **2000 минут/месяц бесплатно** (200 сборок)

**Время:** 5-10 минут на сборку

**Результат:** Профессиональный Windows installer (.exe) готовый к распространению!

---

## 🎉 ГОТОВО!

Теперь у вас есть автоматическая сборка Windows installer!

Любые вопросы - пишите в Issues на GitHub: https://github.com/ВАШ_ЮЗЕРНЕЙМ/xmlPZ/issues

Удачи! 🚀
